-- ============================================================
-- SUPABASE SETUP GUIDE — RUN IN ORDER
-- ============================================================
-- 
-- WHICH FILE TO RUN NOW:
-- ─────────────────────────────────────────────────────────
-- If you get the FK constraint error, run ONLY this file:
--   010_final_cleanup.sql
--
-- If starting fresh (new project), run in this order:
--   001_initial_schema.sql
--   002_seed_data.sql
--   003_mcp_and_helpers.sql
--   008_complete_fix.sql          ← replaces 004,005,006,007
--   009_rag_memory_registry.sql   ← RAG + memory + registry tables
--   010_final_cleanup.sql         ← final idempotent cleanup
--
-- NEVER re-run a migration if it already succeeded.
-- Each migration is idempotent (safe to re-run IF needed).
--
-- ─────────────────────────────────────────────────────────
-- TABLES IN YOUR DATABASE (FULL LIST)
-- ─────────────────────────────────────────────────────────
-- Core:
--   profiles                  User accounts (extends auth.users)
--   agents                    AI agents published to marketplace
--   agent_scores              Quality scores (accuracy, latency, cost, reliability, popularity)
--   agent_registry_versions   Versioned snapshots of published agents
--   agent_memory              Per-user per-agent persistent memory (KV store)
--   agent_embeddings          pgvector embeddings for semantic search
--
-- Execution:
--   executions                Single-agent execution records
--   execution_traces          Full LLM observability (prompts, tokens, cost)
--   pipeline_executions       Multi-agent pipeline execution records
--
-- Pipelines:
--   pipelines                 Multi-agent DAG workflows
--
-- Knowledge / RAG:
--   knowledge_bases           Named collections of documents
--   rag_documents             Individual documents in a knowledge base
--   rag_chunks                pgvector-indexed text chunks
--
-- Commerce:
--   transactions              Payment records (Stripe)
--   agent_subscriptions       Per-agent subscription status
--   payouts                   Seller payout records
--   credits                   User credit balance
--   credit_transactions       Credit debit/credit ledger
--
-- Keys:
--   api_keys                  User API keys (hashed)
--
-- Engagement:
--   reviews                   Agent reviews (1–5 stars)
--   notifications             In-app notifications
--   waitlist                  Pre-launch signups
--   collections               Curated agent lists
--   audit_logs                Immutable event log
--   injection_attempts        Security: prompt injection log
--
-- Analytics:
--   agent_analytics           Daily aggregated per-agent metrics
--
-- ─────────────────────────────────────────────────────────
-- VIEWS (machine-readable)
-- ─────────────────────────────────────────────────────────
--   agents_search             Full-text search with seller info
--   agent_capabilities        Machine-readable capability registry
--   agent_leaderboard         Ranked agents by composite score
--   agent_graph_nodes         Minimal agent data for pipeline DAG engine
--   agent_trace_summary       Per-seller LLM trace analytics
--   user_credit_summary       Per-user credit balance (RLS: own only)
--
-- ─────────────────────────────────────────────────────────
-- CRON JOBS (set up via pg_cron in Supabase)
-- ─────────────────────────────────────────────────────────
-- SELECT cron.schedule('reset-quotas',   '0 0 1 * *',  $$SELECT reset_monthly_quotas()$$);
-- SELECT cron.schedule('score-agents',   '0 2 * * *',  $$SELECT compute_all_agent_scores()$$);
-- SELECT cron.schedule('cleanup-memory', '0 4 * * *',  $$SELECT cleanup_expired_memory()$$);
-- SELECT cron.schedule('daily-analytics','0 1 * * *',  $$SELECT aggregate_daily_analytics()$$);
-- ============================================================
